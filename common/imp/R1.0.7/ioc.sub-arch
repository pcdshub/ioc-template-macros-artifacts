# IOC archive file
## Replace TST:R01:IOC:22 with the PV prefix used for
## iocAdmin PV's on this IOC

# Generate IOC archive request 
$$LOOP(IMP)
file archive_imp$$IF(TYPE,$$TYPE,2).archive
{
	{ IOC = "$$IOC_PV" }
}
$$ENDLOOP(IMP)

$$LOOP(EVR)
file archive_evr_$$IF(TYPE,$$TYPE,2).archive
{
	{ EVR = "$$NAME" }
}
$$ENDLOOP(EVR)
